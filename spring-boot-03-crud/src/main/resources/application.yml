spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/demo?serverTimezone=CTT
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
#      hibernate会帮我们创建表
      ddl-auto: update
    show-sql: true

    spring:
      thymeleaf:
        prefix=classpath:/templates/: